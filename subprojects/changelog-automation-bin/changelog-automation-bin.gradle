plugins {
    id 'groovy'
}

project.description = 'Contains scripts for generating new changelog entries in YAML'

dependencies {
    testImplementation 'junit:junit:4.12'
    testImplementation "org.spockframework:spock-core:1.3-groovy-2.5"
}